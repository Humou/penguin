#pragma once
#include<Vector3f.h>

class Utility
{
public:
	Utility();
	~Utility();
	//bool intersectTri(const Vector3f &origin, const Vector3f &dir, const Vector3f &v0, const Vector3f &v1, const Vector3f &v2, float* t, float* u, float* v);
};

